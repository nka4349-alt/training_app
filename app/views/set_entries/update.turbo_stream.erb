<%= turbo_stream.replace dom_id(@set_entry.workout_exercise),
  partial: "workout_sessions/exercise_card",
  locals: { workout_exercise: @set_entry.workout_exercise, profile: current_user.profile } %>
